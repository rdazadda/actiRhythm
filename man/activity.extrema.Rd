% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_nonparametric_ext.R
\name{activity.extrema}
\alias{activity.extrema}
\title{Generalized Least- and Most-Active Periods (MX / LX)}
\usage{
activity.extrema(counts, timestamps, windows = c(5, 10))
}
\arguments{
\item{counts}{Numeric activity vector (minute epochs recommended).}

\item{timestamps}{POSIXct timestamps, one per value.}

\item{windows}{Window lengths in hours (default \code{c(5, 10)} = L5, M10).}
}
\value{
An object of class \code{actiRhythm_mxlx}: a data frame with, for each
window, the least- and most-active mean level, onset hour, and midpoint hour.
Never errors.
}
\description{
The least-active \code{LX} and most-active \code{MX} periods for arbitrary
window lengths, generalizing L5 and M10 (Van Someren et al. 1999). For each
window the function returns the mean activity over the window and its onset
and midpoint clock times. A fixed L5/M10 pair does not give these phase markers.
}
\examples{
ts <- seq(as.POSIXct("2024-01-01", tz = "UTC"), by = 60, length.out = 4 * 1440)
h  <- as.numeric(format(ts, "\%H"))
activity.extrema(ifelse(h >= 23 | h < 7, 5, 300), ts, windows = c(5, 8, 10))

}
\references{
Van Someren EJW, Swaab DF, Colenda CC, Cohen W, McCall WV, Rosenquist PB
(1999). Bright light therapy: improved sensitivity to its effects on
rest-activity rhythms in Alzheimer patients by application of nonparametric
methods. \emph{Chronobiology International}, 16(4):505-518.
\doi{10.3109/07420529908998724}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_workbook.R
\name{circadian.batch}
\alias{circadian.batch}
\title{Batch Circadian Analysis of Multiple AGD Files}
\usage{
circadian.batch(
  files,
  file = NULL,
  metric = c("axis1", "vm"),
  include_period_ci = FALSE,
  n_boot = 200,
  epoch_length = NULL,
  verbose = TRUE
)
}
\arguments{
\item{files}{Character vector of \code{.agd} paths, or a single directory (all
its \code{.agd} files are used).}

\item{file}{Optional output \code{.xlsx} path for a combined-summary workbook.}

\item{metric}{Activity metric: \code{"axis1"} (default) or \code{"vm"} (vector
magnitude).}

\item{include_period_ci}{Bootstrap the period CI per file (default \code{FALSE}
for batch speed).}

\item{n_boot}{Bootstrap replicates when \code{include_period_ci} is \code{TRUE}.}

\item{epoch_length}{Epoch length in seconds; \code{NULL} (default) infers it per
file from the timestamps.}

\item{verbose}{Print per-file progress (default \code{TRUE}).}
}
\value{
A data frame with one row per file (a \code{file} column, an
\code{error} column, and every summary metric). Returned invisibly when a
workbook \code{file} is written.
}
\description{
Reads a set of ActiGraph \code{.agd} files, runs the full circadian analysis on
each, and returns one combined summary row per file, the package equivalent of
uploading a batch of files. Files that fail to read or analyse are reported in an
\code{error} column rather than aborting the run. Optionally writes a single
Excel workbook whose Summary sheet has one row per file.
}
\examples{
\donttest{
# Pass a folder to analyse a whole batch; a single file is used here.
batch <- circadian.batch(example_agd(), verbose = FALSE)
batch[, c("file", "IS", "IV", "RA", "rhythm_p_value")]
}

}
\seealso{
\code{\link{circadian.workbook}}
}

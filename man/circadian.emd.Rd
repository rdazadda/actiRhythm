% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_emd.R
\name{circadian.emd}
\alias{circadian.emd}
\title{Empirical Mode Decomposition of the Activity Rhythm}
\usage{
circadian.emd(
  counts,
  timestamps,
  max_imf = 10L,
  ensemble = 1L,
  noise_sd = 0.2,
  period_range = c(20, 28),
  epoch_length = 60,
  seed = NULL
)
}
\arguments{
\item{counts}{Numeric activity vector (a coarse epoch is recommended for speed).}

\item{timestamps}{POSIXct timestamps, one per value.}

\item{max_imf}{Maximum number of modes to extract (default 10).}

\item{ensemble}{Ensemble size for EEMD (default 1 = plain EMD).}

\item{noise_sd}{Added-noise SD as a fraction of the series SD (default 0.2).}

\item{period_range}{Period window (hours) for the circadian mode (default 20 to 28).}

\item{epoch_length}{Epoch length in seconds (default 60).}

\item{seed}{Optional seed for the EEMD noise.}
}
\value{
An object of class \code{actiRhythm_emd}: the IMF matrix, the residual
trend, per-IMF period and variance share, the circadian IMF index, and the
reconstruction error. Never errors.
}
\description{
Decomposes the activity series into intrinsic mode functions by empirical mode
decomposition, a data-adaptive, nonlinear alternative to the linear SSA
decomposition (Huang et al. 1998). The intrinsic mode function whose period is
nearest 24 hours is taken as the circadian component. Optional ensemble
EMD adds noise to reduce mode mixing (Wu and Huang 2009).
}
\examples{
ts <- seq(as.POSIXct("2024-01-01", tz = "UTC"), by = 600, length.out = 6 * 144)
th <- as.numeric(difftime(ts, ts[1], units = "hours"))
circadian.emd(100 + 60 * cos(2 * pi * th / 24), ts, epoch_length = 600)

}
\references{
Huang NE, Shen Z, Long SR, et al. (1998). The empirical mode decomposition and
the Hilbert spectrum for nonlinear and non-stationary time series analysis.
\emph{Proceedings of the Royal Society A}, 454(1971):903-995.
\doi{10.1098/rspa.1998.0193}

Wu Z, Huang NE (2009). Ensemble empirical mode decomposition. \emph{Advances in
Adaptive Data Analysis}, 1(1):1-41. \doi{10.1142/S1793536909000047}
}
\seealso{
\code{\link{hilbert.huang}}, \code{\link{circadian.ssa}}
}

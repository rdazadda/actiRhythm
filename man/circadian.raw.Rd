% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_raw.R
\name{circadian.raw}
\alias{circadian.raw}
\title{Circadian Analysis Directly from Raw Acceleration}
\usage{
circadian.raw(
  x,
  metric = c("ENMO", "MAD"),
  device = "auto",
  epoch = 60,
  calibrate = TRUE,
  tz = "UTC",
  ...
)
}
\arguments{
\item{x}{A path to a raw file (\code{.gt3x}, \code{.cwa}, \code{.bin}) or a raw
data frame (see \code{\link{raw.metrics}} / \code{\link{example_raw}}).}

\item{metric}{Raw metric to analyse, \code{"ENMO"} (default) or \code{"MAD"}.}

\item{device}{Device brand or \code{"auto"} (default; used for file input).}

\item{epoch}{Epoch length in seconds (default 60).}

\item{calibrate}{Apply van Hees auto-calibration first (default \code{TRUE}).}

\item{tz}{Time zone (default \code{"UTC"}).}

\item{...}{Passed to \code{\link{circadian.rhythm}}.}
}
\value{
The \code{\link{circadian.rhythm}} result computed on the raw metric.
}
\description{
From a raw accelerometer file (or raw data frame), computes a per-epoch raw
metric (ENMO or MAD) with auto-calibration, then runs the count-style circadian
analysis (\code{\link{circadian.rhythm}}) on it. The metrics (IS, IV, RA,
L5/M10, cosinor, DFA, periodograms, SRI, ...) run on the raw metric the same way
they run on counts, so raw data needs no new per-method code.
}
\examples{
# The full count-style battery on synthetic raw ENMO
\donttest{
cr <- circadian.raw(example_raw(days = 2), metric = "ENMO")
c(IS = cr$IS, IV = cr$IV, RA = cr$RA)
}

}
\seealso{
\code{\link{raw.metrics}}, \code{\link{circadian.rhythm}},
\code{\link{example_raw}}
}

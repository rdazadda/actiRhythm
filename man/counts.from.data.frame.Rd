% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_csv.R
\name{counts.from.data.frame}
\alias{counts.from.data.frame}
\title{Activity Counts from a Data Frame}
\usage{
counts.from.data.frame(
  df,
  count_col = "axis1",
  time_col = NULL,
  epoch_length = 60,
  tz = "UTC",
  start = "1970-01-01"
)
}
\arguments{
\item{df}{A data frame.}

\item{count_col}{Name or index of the count column (default \code{"axis1"}).}

\item{time_col}{Name or index of the timestamp column; if \code{NULL},
timestamps are synthesized from \code{start} by \code{epoch_length} (with a
warning).}

\item{epoch_length}{Epoch length in seconds for synthesized timestamps
(default 60).}

\item{tz}{Time zone (default \code{"UTC"}).}

\item{start}{Start time for synthesized timestamps (default
\code{"1970-01-01"}).}
}
\value{
A data frame with \code{timestamp} (POSIXct) and \code{counts}.
}
\description{
A device-neutral entry point: pull a count column (and optionally a time
column) out of any data frame and return a tidy \code{timestamp}/\code{counts}
frame ready for the analysis functions. Use it for non-ActiGraph counts or any
pre-extracted series.
}
\examples{
df <- data.frame(activity = c(0, 50, 300), clock = c("2024-01-01 00:00:00",
  "2024-01-01 00:01:00", "2024-01-01 00:02:00"))
counts.from.data.frame(df, count_col = "activity", time_col = "clock")

}
\seealso{
\code{\link{read.actigraph.csv}}
}

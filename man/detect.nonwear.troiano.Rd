% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonwear.R
\name{detect.nonwear.troiano}
\alias{detect.nonwear.troiano}
\title{Troiano (2008) Non-Wear Detection}
\usage{
detect.nonwear.troiano(
  counts,
  epoch_length = 60,
  frame = 60,
  spike_tolerance = 2,
  stoplevel = 100
)
}
\arguments{
\item{counts}{Numeric activity vector (vertical axis), minute epochs assumed.}

\item{epoch_length}{Epoch length in seconds (default 60).}

\item{frame}{Minimum non-wear window in minutes (default 60).}

\item{spike_tolerance}{Maximum tolerated nonzero spike in minutes (default 2).}

\item{stoplevel}{Count above which a spike ends the non-wear bout (default 100).}
}
\value{
A logical vector, one per epoch: \code{TRUE} = wear, \code{FALSE} =
non-wear. Never errors.
}
\description{
Classifies each epoch as wear or non-wear with the Troiano et al. (2008,
NHANES) algorithm: a run of at least \code{frame} minutes of zero counts is
non-wear, tolerating up to \code{spike_tolerance} consecutive nonzero minutes
only if every one of them is at or below \code{stoplevel} counts. Unlike Choi
it has no flanking-window requirement but applies a count ceiling on spikes.
}
\examples{
ts <- seq(as.POSIXct("2024-01-01", tz = "UTC"), by = 60, length.out = 400)
counts <- c(rep(150, 100), rep(0, 200), rep(150, 100))
table(detect.nonwear.troiano(counts))

}
\references{
\insertRef{troiano2008}{actiRhythm}
}
\seealso{
\code{\link{detect.nonwear.choi}}
}

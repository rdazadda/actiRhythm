% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_lids.R
\name{lids}
\alias{lids}
\title{Locomotor Inactivity During Sleep (LIDS)}
\usage{
lids(
  counts,
  timestamps,
  sleep_periods,
  epoch_length = 60,
  smooth_minutes = 30,
  period_min = 30,
  period_max = 180,
  period_step = 5
)
}
\arguments{
\item{counts}{Numeric activity vector.}

\item{timestamps}{POSIXct (or parseable) timestamps, one per count.}

\item{sleep_periods}{Data frame with \code{in_bed_time} and \code{out_bed_time}
(same schema as \code{\link{social.jet.lag}}); one LIDS fit per period.}

\item{epoch_length}{Epoch length in seconds (default 60).}

\item{smooth_minutes}{Centered moving-average window in minutes (default 30).}

\item{period_min, period_max, period_step}{Period scan grid in minutes
(defaults 30, 180, 5).}
}
\value{
An object of class \code{actiRhythm_lids}: a per-period data frame
(period in minutes, MRI, amplitude, Pearson r, ...) and the mean period and
MRI across periods.
}
\description{
Quantifies the ultradian (sleep-cycle) oscillation of inactivity during sleep,
following Winnebeck et al. (2018). Within each sleep period the activity is
summed into 10-minute bins, transformed to LIDS (\code{100 / (activity + 1)}),
smoothed with a centered 30-minute moving average, and fit with an ultradian
cosine over a scan of candidate periods; the best period is the interior peak
that maximises the Munich Rhythmicity Index (MRI).
}
\references{
\insertRef{winnebeck2018}{actiRhythm}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_plots.R
\name{plot_mse}
\alias{plot_mse}
\title{Multiscale Entropy Curve}
\usage{
plot_mse(counts, scales = 1:20, m = 2L, r = 0.15)
}
\arguments{
\item{counts}{Numeric activity vector.}

\item{scales}{Integer coarse-graining scales (default \code{1:20}).}

\item{m}{Embedding dimension for sample entropy (default 2).}

\item{r}{Tolerance as a fraction of the series SD (default 0.15).}
}
\value{
A \code{ggplot} object.
}
\description{
Sample entropy from \code{\link{multiscale.entropy}} against the coarse-graining
scale, with the complexity index (area) shaded and the across-scale slope drawn.
A curve that holds its entropy across scales marks a complex signal; one that
falls is closer to noise. Returns a \code{ggplot} object and never errors.
}
\examples{
\donttest{
set.seed(1)
plot_mse(rnorm(3000))
}

}
\references{
\insertRef{costa2002}{actiRhythm}
}

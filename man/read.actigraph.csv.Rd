% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_csv.R
\name{read.actigraph.csv}
\alias{read.actigraph.csv}
\title{Read an ActiGraph (ActiLife) Epoch CSV}
\usage{
read.actigraph.csv(
  filepath,
  tz = "UTC",
  date_format = "\%m/\%d/\%Y",
  epoch_length = 60
)
}
\arguments{
\item{filepath}{Path to the ActiLife CSV.}

\item{tz}{Time zone for the timestamps (default \code{"UTC"}; ActiLife stores
local clock time without a zone).}

\item{date_format}{Date format of the header Start Date (default
\code{"\%m/\%d/\%Y"}, the ActiLife M/d/yyyy default).}

\item{epoch_length}{Fallback epoch length in seconds if the header lacks an
Epoch Period line (default 60).}
}
\value{
A data frame with \code{timestamp} (POSIXct) and the count columns
present (\code{axis1}, \code{axis2}, \code{axis3}, \code{vm}, \code{steps},
\code{lux}).
}
\description{
Reads an ActiLife epoch (count) CSV export. The metadata header is parsed for
the start time and epoch length, the count columns are recognized by name or
position, and a regular timestamp grid is built. The result is a data frame
with a POSIXct \code{timestamp} and the available count columns, the same shape
as \code{\link{agd.counts}}, so it feeds the analysis functions directly.
}
\seealso{
\code{\link{counts.from.data.frame}}, \code{\link{read.agd}}
}

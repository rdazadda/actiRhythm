% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_raw.R
\name{read.raw}
\alias{read.raw}
\title{Activity Counts from a Raw Accelerometer File (Any Supported Brand)}
\usage{
read.raw(
  path,
  device = c("auto", "gt3x", "axivity", "geneactiv"),
  epoch = 60,
  lfe = FALSE,
  tz = "UTC"
)
}
\arguments{
\item{path}{Path to a raw file.}

\item{device}{One of \code{"auto"} (infer from the extension), \code{"gt3x"},
\code{"axivity"}, \code{"geneactiv"}.}

\item{epoch}{Epoch length in seconds (default 60).}

\item{lfe}{Use the low-frequency extension filter (default \code{FALSE}).}

\item{tz}{Time zone for the timestamps (default \code{"UTC"}).}
}
\value{
Data frame with \code{time}, \code{axis1}, \code{axis2}, \code{axis3}
and \code{vm}, one row per epoch. Counts from non-ActiGraph devices are an
approximation; see \code{\link{axivity.counts}} / \code{\link{geneactiv.counts}}.
}
\description{
A single entry point that dispatches to the brand-specific reader by file
extension (or an explicit \code{device}) and returns ActiGraph-equivalent
counts: ActiGraph \code{.gt3x}, Axivity \code{.cwa}, or GENEActiv \code{.bin}.
}
\seealso{
\code{\link{gt3x.counts}}, \code{\link{axivity.counts}},
\code{\link{geneactiv.counts}}
}

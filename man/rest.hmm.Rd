% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_hmm.R
\name{rest.hmm}
\alias{rest.hmm}
\title{State-Space (Hidden Markov) Rest-Activity Model}
\usage{
rest.hmm(
  counts,
  timestamps,
  states = 2L,
  transform = c("sqrt", "log", "none"),
  max_iter = 200L,
  tol = 1e-06,
  n_starts = 5L,
  seed = NULL
)
}
\arguments{
\item{counts}{Numeric activity vector (a coarse epoch is recommended for speed).}

\item{timestamps}{POSIXct timestamps, one per value.}

\item{states}{Number of hidden states (default 2 = rest/active; 3 adds a
moderate state).}

\item{transform}{Variance-stabilizing transform of the counts: \code{"sqrt"}
(default), \code{"log"}, or \code{"none"}.}

\item{max_iter, tol}{EM iteration cap and log-likelihood tolerance.}

\item{n_starts}{Random restarts; the highest-likelihood fit is kept (default 5).}

\item{seed}{Optional seed for the restarts.}
}
\value{
An object of class \code{actiRhythm_hmm}: the per-state emission means
and SDs, the transition matrix, the decoded \code{state_path} and a
\code{sleep_state} ("S"/"W") vector, a 24-hour state-occupancy profile, and
the log-likelihood with AIC/BIC. Never errors.
}
\description{
Fits an unsupervised Gaussian hidden Markov model to the activity counts,
inferring latent rest and active states and the rhythm with which the subject
moves between them (Huang et al. 2018). A threshold scorer labels each
epoch independently; the HMM uses the persistence of states. Its decoded
path gives a 24-hour state-occupancy profile, the probability of being at rest
across the day.
}
\examples{
ts <- seq(as.POSIXct("2024-01-01", tz = "UTC"), by = 600, length.out = 6 * 144)
h  <- as.numeric(format(ts, "\%H"))
counts <- ifelse(h >= 23 | h < 7, 2, 250) + pmax(0, stats::rnorm(length(ts), 0, 10))
rest.hmm(counts, ts)

}
\references{
\insertRef{huang2018hmm}{actiRhythm}
}
\seealso{
\code{\link{sleep.changepoints}}, \code{\link{sleep.cole.kripke}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rest_periods.R
\name{rest.periods}
\alias{rest.periods}
\title{Consolidated Rest-Period Detection (Roenneberg / MASDA)}
\usage{
rest.periods(
  counts,
  timestamps,
  epoch_length = 60,
  trend_period = 86400,
  min_trend_period = 43200,
  threshold = 0.15,
  min_seed_period = 1800,
  max_test_period = 43200,
  r_consec_below = 1800,
  nap_max_minutes = 180
)
}
\arguments{
\item{counts}{Numeric activity vector (minute epochs recommended).}

\item{timestamps}{POSIXct timestamps, one per value.}

\item{epoch_length}{Epoch length in seconds (default 60).}

\item{trend_period}{Moving-average window for the activity trend, in seconds
(default 86400, 24 hours).}

\item{min_trend_period}{Minimum non-missing seconds required in the trend
window (default 43200, 12 hours).}

\item{threshold}{Fraction of the local trend below which an epoch is candidate
rest (default 0.15).}

\item{min_seed_period}{Minimum below-threshold run to seed a bout, in seconds
(default 1800, 30 minutes).}

\item{max_test_period}{Maximum bout length the consolidation tests, in seconds
(default 43200, 12 hours).}

\item{r_consec_below}{Above-threshold tolerance during consolidation, in
seconds (default 1800, 30 minutes).}

\item{nap_max_minutes}{A non-main bout shorter than this is labelled a nap
(default 180).}
}
\value{
An object of class \code{actiRhythm_roenneberg}: a \code{rest_periods}
data frame (one row per consolidated bout, with onset, offset, duration, and
a main/nap label), the per-epoch \code{rest} vector, the activity
\code{trend}, and per-bout counts. The function never errors; with no
resolvable bout it returns an empty \code{rest_periods}.
}
\description{
Detects all consolidated rest bouts across a recording with the Roenneberg
consolidation algorithm (the Munich Actimetry Sleep Detection Algorithm).
Each epoch is compared to a fraction of its own 24-hour activity trend, runs
below that threshold seed candidate rest bouts, and a correlation procedure
grows each seed into a consolidated bout. Unlike \code{\link{sleep.changepoints}}
(one nightly bout per cycle), this returns any number of bouts,
including daytime naps and fragmented or polyphasic rest.
}
\examples{
# Three nights of rest plus one daytime nap
ts <- seq(as.POSIXct("2024-01-01", tz = "UTC"), by = 60, length.out = 3 * 1440)
h  <- as.numeric(format(ts, "\%H")) + as.numeric(format(ts, "\%M")) / 60
day <- as.integer(format(ts, "\%j"))
counts <- ifelse(h >= 23 | h < 7, 5, 300)
counts[h >= 14 & h < 15.5 & day == min(day) + 1L] <- 5   # a nap on day 2
rest.periods(counts, ts)

}
\references{
\insertRef{roenneberg2015}{actiRhythm}

\insertRef{loock2021}{actiRhythm}

\insertRef{hammad2021}{actiRhythm}
}
\seealso{
\code{\link{sleep.changepoints}}, \code{\link{sleep.cole.kripke}}
}

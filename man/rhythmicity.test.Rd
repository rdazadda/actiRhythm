% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_rhythmicity.R
\name{rhythmicity.test}
\alias{rhythmicity.test}
\title{Cosinor Rhythmicity Test}
\usage{
rhythmicity.test(
  counts,
  timestamps,
  period = 24,
  alpha = 0.05,
  wear_time = NULL,
  min_valid_hours = 10,
  cosinor_result = NULL
)
}
\arguments{
\item{counts}{Numeric vector of activity counts (or any activity measure).}

\item{timestamps}{POSIXct timestamps, one per count.}

\item{period}{Rhythm period in hours (default 24).}

\item{alpha}{Significance level for the \code{rhythmic} flag (default 0.05).}

\item{wear_time}{Optional logical wear-time mask passed to the cosinor fit.}

\item{min_valid_hours}{Minimum valid hours per day for the cosinor fit.}

\item{cosinor_result}{Optional existing \code{\link{cosinor.analysis}} result to
test without refitting; when supplied, \code{counts}/\code{timestamps} are
ignored.}
}
\value{
An object of class \code{actiRhythm_rhythmicity}: a list with the F
statistic, numerator/denominator degrees of freedom (\code{df1}, \code{df2}),
\code{p_value}, \code{percent_rhythm} (and \code{r_squared}), a logical
\code{rhythmic} flag, and the cosinor parameters.
}
\description{
Tests whether a rhythm of the given period is present, using the Halberg
zero-amplitude F-test (H0: amplitude = 0) on the single-component cosinor fit,
and reports the percent rhythm (the proportion of variance the cosinor explains,
R^2). It reuses \code{\link{cosinor.analysis}} as the single fitting engine, so
the MESOR/amplitude/acrophase and valid-day gating are identical.
}
\examples{
ts <- seq(as.POSIXct("2024-01-01", tz = "UTC"), by = 60, length.out = 1440)
h <- as.numeric(format(ts, "\%H"))
counts <- pmax(0, 100 + 80 * cos(2 * pi * (h - 14) / 24))
rhythmicity.test(counts, ts)

}
\references{
Nelson W, Tong YL, Lee JK, Halberg F (1979). Methods for cosinor-rhythmometry.
Chronobiologia, 6(4), 305-323.
}

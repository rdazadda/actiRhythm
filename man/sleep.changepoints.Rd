% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circadian_changepoint.R
\name{sleep.changepoints}
\alias{sleep.changepoints}
\title{Change-Point Detection of Sleep and Wake Onsets}
\usage{
sleep.changepoints(
  counts,
  timestamps,
  period = 1440,
  thr = 0.2,
  window_minutes = 240
)
}
\arguments{
\item{counts}{Numeric activity vector (minute epochs recommended).}

\item{timestamps}{POSIXct timestamps, one per value.}

\item{period}{Cosinor period in minutes (default 1440, one day).}

\item{thr}{Dichotomisation threshold on the range-scaled cosine, in
\eqn{[0, 1]} (default 0.2): the fitted curve above \code{thr} is the rough
active span, below it the rough rest span.}

\item{window_minutes}{Half-width of the search window, in minutes, in which
each rough boundary is refined to a change point (default 240).}
}
\value{
An object of class \code{actiRhythm_changepoints}: the cosinor
summary, a \code{changepoints} data frame (time and type, "sleep onset" or
"wake onset"), a \code{sleep_episodes} data frame (sleep onset, wake onset,
and duration in hours), and the mean sleep duration. The function never
errors; on insufficient data it returns the structure with
\code{insufficient = TRUE}.
}
\description{
Locates the sleep-onset and wake-onset time of each circadian cycle by the
cosinor-anchored change-point method of CircaCP (Chen and Sun 2024). A fixed
24-hour cosinor is fitted to bound each rest and active span roughly, and the
precise transition inside each bound is then placed with a single change point
on the raw counts. The result is a per-night sleep-onset / wake-onset table.
A single rest-activity transition rate (such as
\code{\link{state.transitions}}) cannot localise this timing.
}
\examples{
# Five days with a clear active day / restful night
ts <- seq(as.POSIXct("2024-01-01", tz = "UTC"), by = 60, length.out = 5 * 1440)
h  <- as.numeric(format(ts, "\%H"))
counts <- ifelse(h >= 8 & h < 23, 300, 5) + pmax(0, stats::rnorm(length(ts), 0, 5))
sleep.changepoints(counts, ts)

}
\references{
Chen S, Sun X (2024). Validating CircaCP: a generic sleep-wake cycle detection
algorithm for unlabelled actigraphy data. \emph{Royal Society Open Science},
11(5):231468. \doi{10.1098/rsos.231468}

Chen J, Gupta AK, Pan J (2006). Information criterion and change point problem
for regular models. \emph{Sankhya}, 68:252-282.

Killick R, Fearnhead P, Eckley IA (2012). Optimal detection of changepoints
with a linear computational cost. \emph{Journal of the American Statistical
Association}, 107(500):1590-1598. \doi{10.1080/01621459.2012.737745}
}
\seealso{
\code{\link{state.transitions}}, \code{\link{sleep.regularity.index}}
}
